# TeleMed Dr. AI - Environment Variables Example
# Copy this file to .env and fill with your actual values
# NEVER commit .env to version control

# Environment
NODE_ENV=development
PORT=5000

# OpenAI API (required for Dr. AI)
OPENAI_API_KEY=sk-***

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/telemed

# Redis (optional - has in-memory fallback)
REDIS_URL=redis://localhost:6379/0

# AWS S3 (for document storage)
AWS_ACCESS_KEY_ID=***
AWS_SECRET_ACCESS_KEY=***
AWS_REGION=us-east-1
AWS_S3_BUCKET=telemed-documents

# Demo Mode (set to true to use mock data without database)
DEMO_MODE=false

# Security
PSEUDONYM_SALT=*** # for LGPD-compliant pseudonymization

# Monitoring (optional)
GRAFANA_URL=https://your-grafana-instance.com/d/dashboard-id
