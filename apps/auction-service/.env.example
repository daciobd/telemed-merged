# Auction Service (BidConnect) - Environment Variables

# Server
# Use 5001 para evitar conflito com telemed-deploy-ready (porta 5000)
PORT=5001
NODE_ENV=development
SERVICE_NAME=telemed-auction

# JWT Secret (DEVE SER IDÊNTICO ao telemed-internal)
# ⚠️ Use 32+ caracteres aleatórios para segurança
JWT_SECRET=sua_secret_super_secreta_minimo_32_caracteres

# CORS (opcional, aceita todas por padrão em desenvolvimento)
FRONTEND_ORIGIN=*

# Database (Prisma - opcional, usando mock in-memory por padrão)
# DATABASE_URL=postgresql://user:password@localhost:5432/auction

# Internal Services (opcional, para integração avançada)
# INTERNAL_URL_TELEMED=https://telemed-internal.repl.co
# INTERNAL_TOKEN=
