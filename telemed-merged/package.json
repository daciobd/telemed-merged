{
  "name": "telemed-merged",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "pw:browsers": "playwright install --with-deps",
    "start": "node ../apps/telemed-deploy-ready/server.js",
    "build": "echo 'No build needed for static files'",
    "preview": "npx http-server ../. --port 5173 --silent",
    "test:server": "cd .. && npm run dev"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "@types/node": "^20.0.0",
    "http-server": "^14.1.1",
    "wait-on": "^7.2.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}